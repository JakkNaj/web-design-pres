name: Deploy client previews to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            golmansketreninky/package-lock.json
            golmansketreninky-v2/package-lock.json
            golmansketreninky-v3/package-lock.json
            hcricany/package-lock.json
            comsysicearena/package-lock.json
            ceskadopravnistavba/package-lock.json
            donb-garage/package-lock.json
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6
      - name: Build Gólmanské tréninky
        working-directory: golmansketreninky
        env:
          ASTRO_BASE: /web-design-pres/golmansketreninky
        run: |
          npm ci
          npm run build
          node ../scripts/prepare-pages.mjs dist /web-design-pres/golmansketreninky
      - name: Build Gólmanské tréninky — koncept 02
        working-directory: golmansketreninky-v2
        env:
          ASTRO_BASE: /web-design-pres/golmansketreninky-v2
        run: |
          npm ci
          npm test
          npm run build
          python3 scripts/check-built-site.py dist "$ASTRO_BASE/"
      - name: Build Gólmanské tréninky — koncept 03
        working-directory: golmansketreninky-v3
        env:
          ASTRO_BASE: /web-design-pres/golmansketreninky-v3
        run: |
          npm ci
          npm test
          npm run build
          python3 scripts/check-built-site.py dist "$ASTRO_BASE/"
      - name: Build HC Říčany
        working-directory: hcricany
        env:
          ASTRO_BASE: /web-design-pres/hcricany
        run: |
          npm ci
          npm run build
          node ../scripts/prepare-pages.mjs dist /web-design-pres/hcricany
      - name: Build COM-SYS Ice Arena
        working-directory: comsysicearena
        env:
          ASTRO_BASE: /web-design-pres/comsysicearena
        run: |
          npm ci
          npm run build
          node ../scripts/prepare-pages.mjs dist /web-design-pres/comsysicearena
      - name: Build Česká dopravní stavba
        working-directory: ceskadopravnistavba
        env:
          ASTRO_BASE: /web-design-pres/ceskadopravnistavba
        run: |
          npm ci
          npm run build
          node ../scripts/prepare-pages.mjs dist /web-design-pres/ceskadopravnistavba
      - name: Build DON B Garage
        working-directory: donb-garage
        env:
          ASTRO_BASE: /web-design-pres/donb-garage
        run: |
          npm ci
          npm run build
          node ../scripts/prepare-pages.mjs dist /web-design-pres/donb-garage
      - name: Assemble Pages artifact
        run: |
          mkdir -p _site/golmansketreninky _site/golmansketreninky-v2 _site/golmansketreninky-v3 _site/hcricany _site/comsysicearena _site/ceskadopravnistavba _site/donb-garage
          cp index.html _site/index.html
          cp -R golmansketreninky/dist/. _site/golmansketreninky/
          cp -R golmansketreninky-v2/dist/. _site/golmansketreninky-v2/
          cp -R golmansketreninky-v3/dist/. _site/golmansketreninky-v3/
          cp -R hcricany/dist/. _site/hcricany/
          cp -R comsysicearena/dist/. _site/comsysicearena/
          cp -R ceskadopravnistavba/dist/. _site/ceskadopravnistavba/
          cp -R donb-garage/dist/. _site/donb-garage/
      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
